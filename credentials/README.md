# credentials

Place your Google JSON here:

- `service_account.json` — Service Account key (recommended)
- `oauth.json` — OAuth client (alternative)
- `token.json` — auto-generated after OAuth flow (do not commit)

This folder is gitignored.
